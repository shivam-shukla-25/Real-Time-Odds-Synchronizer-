## 1. Backend

cd backend

npm install

npm run dev

The socket server listens on `http://localhost:4000` and broadcasts and `odds:update` event every 3 seconds.

## 2. Frontend

In a second terminal:

cd frontend

npm install

npm run dev

The app is available at `http://localhost:3000`.# Real-Time-Odds-Synchronizer-
